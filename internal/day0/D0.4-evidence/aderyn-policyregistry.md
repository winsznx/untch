<!--
==============================================================================
 EVIDENCE — Aderyn (Cyfrin) static analysis on PolicyRegistry
 PRD §28 static stage. First real contract (§10.1). Generated 2026-07-09.
==============================================================================
 INSTALL METHOD : npm — `@cyfrin/aderyn@0.6.8`, run via `npx` locally. This is
                  the SAME install path and version the CI gate now uses:
                  .github/workflows/contracts.yml installs Aderyn through
                  `Cyfrin/aderyn-ci@v0`, which runs `npm install -g
                  @cyfrin/aderyn@0.6` (→ 0.6.8). The scaffold-era evidence used
                  `cargo install aderyn` @ 0.1.9; that from-source path is
                  retired because a transitive dep (svm-rs-builds) currently
                  fails to compile in CI (alloy-rs/svm-rs #177, fix #178).
 TOOL VERSION   : aderyn 0.6.8   (was 0.1.9 for the scaffold — deliberate bump
                  to match the maintainer's CI action)
 SOLC           : 0.8.34  (pinned via contracts/foundry.toml; fetched by aderyn)
 EVM VERSION    : paris   (from foundry.toml; echoed in aderyn's run header)
 CONFIG         : aderyn.toml at the repo root — version=1, root="contracts",
                  src="src/". aderyn-ci runs `aderyn .` from the repo root and
                  discourages its `working-directory:` input, so project scoping
                  lives in this toml. src="src/" keeps analysis to first-party
                  sources (excludes lib/forge-std), matching the retired
                  `aderyn --src src/` invocation.
 COMMAND        : aderyn . -o <report> --no-snippets --skip-update-check
                  (CI variant additionally emits JSON and gates on
                  .issue_count.high via `fail-on: high`)
 GATE RULE      : CI fails if Aderyn reports any HIGH issue (PRD §28: "High
                  findings block"; Low do not block). Enforced by
                  `Cyfrin/aderyn-ci@v0` with `fail-on: high`.
 RESULT         : high = 0, low = 0   (GATE PASS)
                  88 detectors run over 2 files (153 nSLOC).

 WHY THIS REPORT IS A LOCAL RUN (evidence continuity):
   Cyfrin/aderyn-ci@v0 generates its md+json reports internally and then
   deletes them (`rmRF`) after gating — it exposes no artifact or output to
   archive. So the readable evidence below is produced by a local Aderyn run
   pinned to the exact version the CI gate installs (0.6.8), then committed
   here. This is continuous with the scaffold-era pattern, which was likewise
   a locally-generated report committed as evidence — only the install method
   (cargo → npm) and version (0.1.9 → 0.6.8) changed, both to track the CI gate.

 CROSS-TOOL DISPOSITION (PRD §28 "no silent disagreement"):
   Slither (gated separately in the same job, `--fail-medium` against
   slither.triage.json) and Aderyn agree: no High/Medium findings on
   PolicyRegistry. Aderyn additionally reports 0 Low here (the scaffold had 2).
==============================================================================
-->

# Aderyn Analysis Report

This report was generated by [Aderyn](https://github.com/Cyfrin/aderyn), a static analysis tool built by [Cyfrin](https://cyfrin.io), a blockchain security company. This report is not a substitute for manual audit or security review. It should not be relied upon for any purpose other than to assist in the identification of potential security vulnerabilities.
# Table of Contents

- [Summary](#summary)
  - [Files Summary](#files-summary)
  - [Files Details](#files-details)
  - [Issue Summary](#issue-summary)


# Summary

## Files Summary

| Key | Value |
| --- | --- |
| .sol Files | 2 |
| Total nSLOC | 153 |


## Files Details

| Filepath | nSLOC |
| --- | --- |
| src/PolicyRegistry.sol | 120 |
| src/lib/IntentHash.sol | 33 |
| **Total** | **153** |


## Issue Summary

| Category | No. of Issues |
| --- | --- |
| High | 0 |
| Low | 0 |
