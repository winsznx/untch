{
  "name": "@untch/consumer-providers",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "description": "Untch Consumer Pack provider layer — the typed adapter contract, the hardened outbound HTTP client, the x402 / MPP / SIWX protocol clients, and the verified merchant adapters.",
  "scripts": {
    "test": "node --import tsx --test test/*.test.ts"
  },
  "dependencies": {
    "@solana-program/token": "^0.9.0",
    "@solana/kit": "^5.5.1",
    "@untch/consumer-core": "workspace:*",
    "@untch/shared": "workspace:*",
    "@untch/x402-guard": "workspace:*",
    "@x402/fetch": "2.20.0",
    "@x402/svm": "2.20.0",
    "viem": "^2.55.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
