{
  "name": "@untch/proof-engine",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Untch Proof Engine (PRD §13 / §7.3). PARTIAL SLICE — see README: only T0 (deterministic schema/conformance verification: ajv schema + field/size/regex + exact-hash for deterministic deliverables, plus acceptance-criteria binding and the VERIFY_SKIPPED_UNCOMMITTED buyer-hygiene path) is real. T1–T4 are explicit NOT_IMPLEMENTED stubs surfaced in every result, never silently absent and never faked as PASS. No LLM anywhere (invariant I1).",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test test/*.test.ts"
  },
  "dependencies": {
    "@untch/canon": "workspace:*",
    "ajv": "^8.17.1",
    "viem": "^2.55.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.7.3"
  }
}
