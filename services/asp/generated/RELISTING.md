# Untch — proposed relisting for existing OKX ASP #6086

GENERATED by `pnpm gen:schemas`. Do not edit by hand.

This updates the EXISTING ASP. It does not register a new one, and it never uses the ERC-8004
agent id (6047) in the ASP position.

| | |
| --- | --- |
| OKX ASP id | 6086 |
| ASP type | A2MCP |
| ERC-8004 agent id | 6047 — on-chain identity only |
| Production URL | https://asp.untch.xyz |
| Docs | https://docs.untch.xyz |
| Network | eip155:196 |
| Settlement token | USDT0 `0x779ded0c9e1022225f8e0630b35a9b54be713736` |
| payTo | `0xD9eD4D474B0D01031d10d637546450F39ed6a5ba` |
| Payment SDK | @okxweb3/x402-express@0.1.1, scheme `exact` |
| Profile picture | 1024x1024 image/png |

## Description

A live control plane for agent money. The model never touches the money.

Untch is a live control plane for agent money. Agents submit exact commercial intents. Deterministic policy automatically approves, blocks or escalates them. Humans retain account and wallet authority. Approved requests create bounded reserved authority rather than broad wallet access.

Untch provides:

- deterministic policy evaluation
- automatic approval, blocking and escalation
- per-call and daily budgets
- provider and capability controls
- duplicate and replay protection
- exact quote binding
- paid x402 control services
- human approval through live supported channels
- reserved authority accounting
- quote lineage and atomic supersession
- decision and payment evidence

## Services

9 listed — 6 paid, 3 free.

| Service | Method | Endpoint | Price | What the caller receives |
| --- | --- | --- | --- | --- |
| Policy preflight | POST | /preflight_payment | $0.05 (50000 base units) | a decision, the ordered list of rules that were evaluated and what each one found, and a receipt reference for the decision |
| Delivery verify | POST | /verify_delivery | $0.10 (100000 base units) | a pass or fail verdict against the committed criteria, and a durable receipt of that verdict |
| Duplicate check | POST | /detect_duplicate | $0.02 (20000 base units) | whether a matching prior intent exists, which one, and how long the match remains in the window |
| Metadata redaction | POST | /redact_payment_metadata | $0.02 (20000 base units) | the redacted metadata and a hash of it, with nothing stored |
| Product name suggestions | POST | /builder/suggest_names | $0.01 (10000 base units) | a handful of candidate names, each with the reasoning behind it |
| Name ranking | POST | /builder/rank_options | free | the names in ranked order with their scores |
| Domain availability | POST | /builder/check_domains | free | one availability verdict per domain, with the registry response behind it |
| Launch checklist | POST | /builder/seo_tips | free | a list of concrete steps, using the name given |
| Launch brand pack | POST | /builder/brand_pack | $0.05 (50000 base units) | candidate names, their domain verdicts, a ranking, and a launch checklist |

## Approval channels

- **discord** — live — delivery and one-tap approve/deny, proven against real requests
- **web** — live — signed single-use action links
- **telegram** — disabled — the stored bot credential is not authorised

## Not listed, and why

19 of this host's 28 services are deliberately not marketplace products.

| Service | Class | Reason |
| --- | --- | --- |
| `catalog` | PUBLIC_SUPPORT | The free index of what this service serves. Discovery, not a product — nobody buys a list of things they could buy. |
| `ping_untch` | PUBLIC_SUPPORT | A health check. It used to cost $0.01, which billed a buyer to prove that x402 works rather than to receive anything. Free, useful, and not a deliverable. |
| `create_spend_intent` | ACCOUNT_CONTROL | It writes an intent into this host's store against a policy the caller must own. Free, correct, and part of operating an Untch account rather than something a stranger buys. |
| `get_ledger` | ACCOUNT_CONTROL | It reads this host's ledger window for a policy the caller must own. Exposing an account's own spend history as an open marketplace service would be publishing private state. |
| `log_receipt` | ACCOUNT_CONTROL | It appends to this host's receipt store for a policy the caller must own. Writing into somebody's account is account control, whatever it costs. |
| `receipt_status` | ACCOUNT_CONTROL | It resolves a receiptId that only a prior account-bound write produces. A stranger has no receiptId to ask about, so being free does not make it a service anyone could use. |
| `escalation_status` | ACCOUNT_CONTROL | It resolves an opaque pollRef issued by a prior escalation on this host. Same shape as receipt status: without the reference there is nothing to poll, and the reference is account-bound. |
| `score_vendor` | INTERNAL_OR_WITHHELD | It scores a vendor from receipt history held by this host. A stranger's vendor has no history here, so the answer cannot exist. The route now refuses before any payment challenge is emitted rather than charging for a question it cannot answer. |
| `score_buyer` | INTERNAL_OR_WITHHELD | It scores a buyer from receipt history held by this host, and refuses before any payment challenge for the same reason vendor scoring does. |
| `generate_dispute_packet` | INTERNAL_OR_WITHHELD | It assembles a dispute packet from an intentHash this host has history for. Without that history there is nothing to assemble, so it refuses before a challenge rather than after settlement. |
| `reconcile_agent_spend` | INTERNAL_OR_WITHHELD | It reconciles receipt and ledger history for an agent over a period, all of which is held here. A caller with no history gets a refusal before any payment challenge. |
| `cafe_menu` | PUBLIC_SUPPORT | The menu for the cafe demonstration. Free, public, and part of a simulation rather than a service. |
| `cafe_order_latte` | PRODUCTION_DISABLED | It simulates a coffee order. No merchant is contacted, no order is placed and no coffee exists. It used to cost $0.04, which made a demonstration look like a purchase. Kept as a free, clearly labelled simulation and excluded from the marketplace, because a listing that sells a simulated latte is a listing that misrepresents what a payment buys. |
| `account_link_start` | ACCOUNT_CONTROL | It begins binding a wallet to an Untch account. This is this project's own sign-in, and listing it would be advertising account setup as a purchasable service. |
| `account_link_complete` | ACCOUNT_CONTROL | It completes a wallet binding against a signature. The other half of sign-in, and not a product. |
| `policy_draft` | ACCOUNT_CONTROL | It builds an unsigned policy registration for the caller to send from their own wallet. A setup step for an account, correctly free, and not something a stranger buys. |
| `policy_sync` | ACCOUNT_CONTROL | It records a policy registration the caller already sent from their own wallet. The second half of setup, and account control for the same reason the first half is. |
| `set_default_policy` | ACCOUNT_CONTROL | It sets which policy an account uses by default. Configuration of somebody's account. |
| `approval_decide` | ACCOUNT_CONTROL | A legacy human control route, replaced by the bound-action path for service-call-backed approvals. It requires account state and refuses a modern paid approval request that has no bound action, so a stranger calling it can only ever be refused. Kept mounted for compatibility, marked deprecated, and removed from the listing where it was advertised as a free marketplace service. |

## Reviewer instructions

```
Every listed service can be checked from outside. No Untch account is needed for any step below.

1. The contract, free to read, before spending anything:
   GET https://asp.untch.xyz/catalog
   GET https://asp.untch.xyz/schema/<toolId>
   GET https://asp.untch.xyz/.well-known/x402
   GET https://asp.untch.xyz/payment-sdk-health   (per-route proof the official SDK is in the paid path)

2. The free services return their real result directly:
   POST https://asp.untch.xyz/builder/rank_options   → the names in ranked order with their scores
   POST https://asp.untch.xyz/builder/check_domains   → one availability verdict per domain, with the registry response behind it
   POST https://asp.untch.xyz/builder/seo_tips   → a list of concrete steps, using the name given

3. The paid services answer 402 until paid, with the price, chain, token and payee in the
   PAYMENT-REQUIRED header:
   POST https://asp.untch.xyz/preflight_payment   → 402, $0.05 (50000 base units)
   POST https://asp.untch.xyz/verify_delivery   → 402, $0.10 (100000 base units)
   POST https://asp.untch.xyz/detect_duplicate   → 402, $0.02 (20000 base units)
   POST https://asp.untch.xyz/redact_payment_metadata   → 402, $0.02 (20000 base units)
   POST https://asp.untch.xyz/builder/suggest_names   → 402, $0.01 (10000 base units)
   POST https://asp.untch.xyz/builder/brand_pack   → 402, $0.05 (50000 base units)

   network eip155:196 · token 0x779ded0c9e1022225f8e0630b35a9b54be713736 (USDT0) · payTo 0xD9eD4D474B0D01031d10d637546450F39ed6a5ba

4. Pay one and replay it. The response carries the promised result and the settlement evidence.

Two of the paid services need something that exists before the call, and both say so in their
own contract rather than only here:
   preflight_payment needs a registered spend policy. Build it with POST https://asp.untch.xyz/consumer/policies/draft,
   send that transaction from your own wallet, then POST https://asp.untch.xyz/consumer/policies/sync.
   Untch does not relay the call and cannot — the policy's owner is whoever sent it.
   verify_delivery needs an intent created on this host, which preflight_payment returns.
```

## The relisting instruction

```
Use Onchain OS and my logged-in Agentic Wallet to update and relist the existing A2MCP ASP #6086 using the approved canonical payload at services/asp/generated/relisting-payload.json. Do not register a new ASP. Do not use 6047 — that is the ERC-8004 agent id, not the ASP id.
```
