# Untch service contracts

GENERATED by `pnpm gen:schemas` from `services/asp/src/registry/services.ts`. Do not edit by hand.

Every service this host offers a stranger's agent. Each entry is the same definition the runtime
validator, the OpenAPI document, the x402 discovery document and the listing description are built
from — so there is no version of this page that can disagree with the service.

| Tool | Price | Maturity | Contract |
| --- | --- | --- | --- |
| `catalog` | free | live | [/schema/catalog](https://asp.untch.xyz/schema/catalog) |
| `ping_untch` | $0.01 | live | [/schema/ping_untch](https://asp.untch.xyz/schema/ping_untch) |
| `preflight_payment` | $0.05 | blocked | [/schema/preflight_payment](https://asp.untch.xyz/schema/preflight_payment) |
| `verify_delivery` | $0.10 | blocked | [/schema/verify_delivery](https://asp.untch.xyz/schema/verify_delivery) |
| `create_spend_intent` | free | blocked | [/schema/create_spend_intent](https://asp.untch.xyz/schema/create_spend_intent) |
| `get_ledger` | free | demo | [/schema/get_ledger](https://asp.untch.xyz/schema/get_ledger) |
| `detect_duplicate` | $0.02 | demo | [/schema/detect_duplicate](https://asp.untch.xyz/schema/detect_duplicate) |
| `redact_payment_metadata` | $0.02 | live | [/schema/redact_payment_metadata](https://asp.untch.xyz/schema/redact_payment_metadata) |
| `log_receipt` | free | live | [/schema/log_receipt](https://asp.untch.xyz/schema/log_receipt) |
| `receipt_status` | free | live | [/schema/receipt_status](https://asp.untch.xyz/schema/receipt_status) |
| `escalation_status` | free | live | [/schema/escalation_status](https://asp.untch.xyz/schema/escalation_status) |
| `score_vendor` | $0.20 | blocked | [/schema/score_vendor](https://asp.untch.xyz/schema/score_vendor) |
| `score_buyer` | $0.20 | blocked | [/schema/score_buyer](https://asp.untch.xyz/schema/score_buyer) |
| `generate_dispute_packet` | $0.50 | blocked | [/schema/generate_dispute_packet](https://asp.untch.xyz/schema/generate_dispute_packet) |
| `reconcile_agent_spend` | $0.25 | blocked | [/schema/reconcile_agent_spend](https://asp.untch.xyz/schema/reconcile_agent_spend) |
| `cafe_menu` | free | demo | [/schema/cafe_menu](https://asp.untch.xyz/schema/cafe_menu) |
| `cafe_order_latte` | $0.04 | demo | [/schema/cafe_order_latte](https://asp.untch.xyz/schema/cafe_order_latte) |
| `suggest_names` | $0.01 | demo | [/schema/suggest_names](https://asp.untch.xyz/schema/suggest_names) |
| `rank_options` | free | blocked | [/schema/rank_options](https://asp.untch.xyz/schema/rank_options) |
| `check_domains` | free | blocked | [/schema/check_domains](https://asp.untch.xyz/schema/check_domains) |
| `seo_tips` | free | demo | [/schema/seo_tips](https://asp.untch.xyz/schema/seo_tips) |
| `brand_pack` | $0.05 | blocked | [/schema/brand_pack](https://asp.untch.xyz/schema/brand_pack) |

## Service catalog — `catalog`

`GET /catalog` · free · schema v1.0.0

Lists every tool this host serves, with its route, price and purpose. For any agent deciding whether this host has a tool worth calling.

You provide: Nothing. This service takes no parameters.

You receive: a JSON catalog of every route, its price, and what it is for.

**Repeating an identical request:** read-only.

**Refusals**

- `METHOD_NOT_ALLOWED` (405) — the path is right and the verb is not

**A request that works**

```json
null
```

**A request that is refused** — `METHOD_NOT_ALLOWED`

```json
{
  "anything": true
}
```

## Rail ping — `ping_untch`

`GET /ping_untch` · $0.01 per call · schema v1.0.0

Confirms that a paid call to this host settles end to end. For an integrator proving their x402 client can pay this host before wiring anything expensive.

You provide: Nothing. This service takes no parameters.

You receive: a timestamped acknowledgement, after a real settled payment of $0.01.

**Must already exist**

- An x402 client holding USDT0 on X Layer mainnet. The call settles a real payment before the handler runs. Obtain it with: any x402-capable wallet funded with USDT0 on eip155:196.

**What it changes**

- Settles $0.01 of USDT0 to this host's payTo address.

**Repeating an identical request:** not-idempotent.

**Refusals**

- `PAYMENT_REQUIRED` (402) — no valid payment accompanied the request

**A request that works**

```json
null
```

**A request that is refused** — `PAYMENT_REQUIRED`

```json
null
```

## Policy preflight — `preflight_payment`

`POST /preflight_payment` · $0.05 per call · schema v1.0.0

Judges a proposed payment against a registered spend policy and returns allow, block or escalate, with the rule that decided it. For an operator funding an autonomous agent who wants every payment checked before it moves.

You provide: policyId (string); either intent, containing all 16 of: owner, buyerAgentId, workerAgentId, token, maxAmount, taskHash, acceptanceHash, schemaHash, policyHash, deadline, nonce, endpoint, paramsHash, recipientAddress, category, amount, or intentHash (string).

You receive: a decision, the ordered list of rules that were evaluated and what each one found, and a receipt reference for the decision.

> **Not listable.** This service cannot be completed by a caller who does not already
> hold something no public route produces:
> - A registered spend policy, and its numeric policyId.
> - The policyHash of that policy — the exact hash the registry stored when it was registered.

**Must already exist**

- A registered spend policy, and its numeric policyId. Every decision this service makes is a comparison against a policy. Without one there is nothing to compare to, and the request is refused rather than judged. **No public route produces this.**
- The policyHash of that policy — the exact hash the registry stored when it was registered. It binds the request to one VERSION of the rules, so a policy edited after the request was built cannot silently change the answer. **No public route produces this.**

**What it changes**

- Records the decision in this instance's rolling ledger window. (does not outlive the request)
- Writes a DECISION receipt and queues it for anchoring, when a receipt writer is wired.

**Repeating an identical request:** not-idempotent.

**Refusals**

- `PAYMENT_REQUIRED` (402) — no valid payment accompanied the request
- `POLICY_ID_REQUIRED` (400) — policyId is absent or is not a decimal string
- `INTENT_REQUIRED` (400) — neither an inline intent nor an intentHash was given
- `INTENT_MALFORMED` (400) — a field of the intent has the wrong type or format
- `INTENT_NOT_FOUND` (404) — the intentHash is unknown to THIS instance
- `POLICY_NOT_FOUND` (404) — no policy with that id is stored
- `POLICY_BINDING_MISMATCH` (400) — policyHash does not equal the stored policy's hash
- `POLICY_STORE_NOT_CONFIGURED` (503) — this instance has no policy store

**A request that works**

```json
{
  "policyId": "7",
  "intent": {
    "owner": "0xd9ed4d474b0d01031d10d637546450f39ed6a5ba",
    "buyerAgentId": "6047",
    "workerAgentId": "6086",
    "token": "0x779ded0c9e1022225f8e0630b35a9b54be713736",
    "maxAmount": "2000000",
    "taskHash": "0x1111111111111111111111111111111111111111111111111111111111111111",
    "acceptanceHash": "0x2222222222222222222222222222222222222222222222222222222222222222",
    "schemaHash": "0x3333333333333333333333333333333333333333333333333333333333333333",
    "policyHash": "0x4444444444444444444444444444444444444444444444444444444444444444",
    "deadline": "1790000000",
    "nonce": "1",
    "endpoint": "https://asp.untch.xyz/ping_untch",
    "paramsHash": "0x5555555555555555555555555555555555555555555555555555555555555555",
    "recipientAddress": "0xd9ed4d474b0d01031d10d637546450f39ed6a5ba",
    "category": "api",
    "amount": 1.5
  }
}
```

**A request that is refused** — `INTENT_MALFORMED`

```json
{
  "policyId": "7",
  "intent": {
    "owner": "0xd9ed4d474b0d01031d10d637546450f39ed6a5ba",
    "buyerAgentId": "6047",
    "workerAgentId": "6086",
    "token": "0x779ded0c9e1022225f8e0630b35a9b54be713736",
    "maxAmount": 2000000,
    "taskHash": "0x1111111111111111111111111111111111111111111111111111111111111111",
    "acceptanceHash": "0x2222222222222222222222222222222222222222222222222222222222222222",
    "schemaHash": "0x3333333333333333333333333333333333333333333333333333333333333333",
    "policyHash": "0x4444444444444444444444444444444444444444444444444444444444444444",
    "deadline": "1790000000",
    "nonce": "1",
    "endpoint": "https://asp.untch.xyz/ping_untch",
    "paramsHash": "0x5555555555555555555555555555555555555555555555555555555555555555",
    "recipientAddress": "0xd9ed4d474b0d01031d10d637546450f39ed6a5ba",
    "category": "api",
    "amount": 1.5
  }
}
```

## Delivery verify — `verify_delivery`

`POST /verify_delivery` · $0.10 per call · schema v1.0.0

Checks a delivered result against the acceptance criteria that were committed to before the work started, and records the verdict. For a buyer deciding whether work they commissioned has actually been delivered.

You provide: policyId (string); either intent, containing all 16 of: owner, buyerAgentId, workerAgentId, token, maxAmount, taskHash, acceptanceHash, schemaHash, policyHash, deadline, nonce, endpoint, paramsHash, recipientAddress, category, amount, or intentHash (string); either payload (object), or payloadHash (string).

You receive: a pass or fail verdict against the committed criteria, and a durable receipt of that verdict.

> **Not listable.** This service cannot be completed by a caller who does not already
> hold something no public route produces:
> - A registered spend policy, and its numeric policyId.
> - The policyHash of that policy — the exact hash the registry stored when it was registered.
> - The acceptanceHash that was committed when the work was commissioned.

**Must already exist**

- A registered spend policy, and its numeric policyId. Every decision this service makes is a comparison against a policy. Without one there is nothing to compare to, and the request is refused rather than judged. **No public route produces this.**
- The policyHash of that policy — the exact hash the registry stored when it was registered. It binds the request to one VERSION of the rules, so a policy edited after the request was built cannot silently change the answer. **No public route produces this.**
- The acceptanceHash that was committed when the work was commissioned. Verification compares against what was agreed BEFORE the work, which is the only comparison that means anything afterwards. **No public route produces this.**

**What it changes**

- Writes a VERIFY receipt and queues it for on-chain anchoring. Calling twice writes twice.

**Repeating an identical request:** not-idempotent.

**Refusals**

- `PAYMENT_REQUIRED` (402) — no valid payment accompanied the request
- `POLICY_ID_REQUIRED` (400) — policyId is absent or is not a decimal string
- `INTENT_REQUIRED` (400) — neither an inline intent nor an intentHash was given
- `INTENT_NOT_FOUND` (404) — the intentHash is unknown to THIS instance
- `POLICY_NOT_FOUND` (404) — no policy with that id is stored
- `POLICY_BINDING_MISMATCH` (400) — policyHash does not equal the stored policy's hash
- `CRITERIA_MALFORMED` (400) — acceptanceCriteria was given but is not an object
- `DELIVERY_REQUIRED` (400) — neither payload nor payloadHash was given
- `DELIVERY_MALFORMED` (400) — payloadHash is not a 32-byte hex string

**A request that works**

```json
{
  "policyId": "7",
  "intent": {
    "owner": "0xd9ed4d474b0d01031d10d637546450f39ed6a5ba",
    "buyerAgentId": "6047",
    "workerAgentId": "6086",
    "token": "0x779ded0c9e1022225f8e0630b35a9b54be713736",
    "maxAmount": "2000000",
    "taskHash": "0x1111111111111111111111111111111111111111111111111111111111111111",
    "acceptanceHash": "0x2222222222222222222222222222222222222222222222222222222222222222",
    "schemaHash": "0x3333333333333333333333333333333333333333333333333333333333333333",
    "policyHash": "0x4444444444444444444444444444444444444444444444444444444444444444",
    "deadline": "1790000000",
    "nonce": "1",
    "endpoint": "https://asp.untch.xyz/ping_untch",
    "paramsHash": "0x5555555555555555555555555555555555555555555555555555555555555555",
    "recipientAddress": "0xd9ed4d474b0d01031d10d637546450f39ed6a5ba",
    "category": "api",
    "amount": 1.5
  },
  "payloadHash": "0x6666666666666666666666666666666666666666666666666666666666666666"
}
```

**A request that is refused** — `DELIVERY_REQUIRED`

```json
{
  "policyId": "7",
  "intent": {
    "owner": "0xd9ed4d474b0d01031d10d637546450f39ed6a5ba",
    "buyerAgentId": "6047",
    "workerAgentId": "6086",
    "token": "0x779ded0c9e1022225f8e0630b35a9b54be713736",
    "maxAmount": "2000000",
    "taskHash": "0x1111111111111111111111111111111111111111111111111111111111111111",
    "acceptanceHash": "0x2222222222222222222222222222222222222222222222222222222222222222",
    "schemaHash": "0x3333333333333333333333333333333333333333333333333333333333333333",
    "policyHash": "0x4444444444444444444444444444444444444444444444444444444444444444",
    "deadline": "1790000000",
    "nonce": "1",
    "endpoint": "https://asp.untch.xyz/ping_untch",
    "paramsHash": "0x5555555555555555555555555555555555555555555555555555555555555555",
    "recipientAddress": "0xd9ed4d474b0d01031d10d637546450f39ed6a5ba",
    "category": "api",
    "amount": 1.5
  }
}
```

## Create spend intent — `create_spend_intent`

`POST /create_spend_intent` · free · schema v1.0.0

Canonicalises and hashes a proposed payment, binding it to a registered policy. For an agent that wants one stable hash to refer to a payment through preflight, verification and receipt.

You provide: policyId (string); intent, containing all 16 of: owner, buyerAgentId, workerAgentId, token, maxAmount, taskHash, acceptanceHash, schemaHash, policyHash, deadline, nonce, endpoint, paramsHash, recipientAddress, category, amount.

You receive: the canonical form of the intent and its intentHash.

> **Not listable.** This service cannot be completed by a caller who does not already
> hold something no public route produces:
> - A registered spend policy, and its numeric policyId.
> - The policyHash of that policy — the exact hash the registry stored when it was registered.

**Must already exist**

- A registered spend policy, and its numeric policyId. Every decision this service makes is a comparison against a policy. Without one there is nothing to compare to, and the request is refused rather than judged. **No public route produces this.**
- The policyHash of that policy — the exact hash the registry stored when it was registered. It binds the request to one VERSION of the rules, so a policy edited after the request was built cannot silently change the answer. **No public route produces this.**

**What it changes**

- Caches the intent IN MEMORY on this instance only. It does not survive a restart and is invisible to any other instance. (does not outlive the request)

**Repeating an identical request:** idempotent.

**Refusals**

- `POLICY_ID_REQUIRED` (400) — policyId is absent or is not a decimal string
- `INTENT_REQUIRED` (400) — no intent was given
- `INTENT_MALFORMED` (400) — a field of the intent has the wrong type or format
- `POLICY_NOT_FOUND` (404) — no policy with that id is stored
- `POLICY_STORE_NOT_CONFIGURED` (503) — this instance has no policy store

**A request that works**

```json
{
  "policyId": "7",
  "intent": {
    "owner": "0xd9ed4d474b0d01031d10d637546450f39ed6a5ba",
    "buyerAgentId": "6047",
    "workerAgentId": "6086",
    "token": "0x779ded0c9e1022225f8e0630b35a9b54be713736",
    "maxAmount": "2000000",
    "taskHash": "0x1111111111111111111111111111111111111111111111111111111111111111",
    "acceptanceHash": "0x2222222222222222222222222222222222222222222222222222222222222222",
    "schemaHash": "0x3333333333333333333333333333333333333333333333333333333333333333",
    "policyHash": "0x4444444444444444444444444444444444444444444444444444444444444444",
    "deadline": "1790000000",
    "nonce": "1",
    "endpoint": "https://asp.untch.xyz/ping_untch",
    "paramsHash": "0x5555555555555555555555555555555555555555555555555555555555555555",
    "recipientAddress": "0xd9ed4d474b0d01031d10d637546450f39ed6a5ba",
    "category": "api",
    "amount": 1.5
  }
}
```

**A request that is refused** — `INTENT_REQUIRED`

```json
{
  "policyId": "7"
}
```

## Spend window — `get_ledger`

`POST /get_ledger` · free · schema v1.0.0

Returns the recent spend window this instance is holding for one policy. For an operator checking what a policy has spent recently.

You provide: policyId (string).

You receive: the spend-so-far, the call count in the last hour, and the most recent intents.

> **Not listable.** This service cannot be completed by a caller who does not already
> hold something no public route produces:
> - A registered spend policy, and its numeric policyId.

**Must already exist**

- A registered spend policy, and its numeric policyId. Every decision this service makes is a comparison against a policy. Without one there is nothing to compare to, and the request is refused rather than judged. **No public route produces this.**

**Repeating an identical request:** read-only.

**Refusals**

- `POLICY_ID_REQUIRED` (400) — policyId is absent or empty

**A request that works**

```json
{
  "policyId": "7"
}
```

**A request that is refused** — `POLICY_ID_REQUIRED`

```json
{}
```

## Duplicate check — `detect_duplicate`

`POST /detect_duplicate` · $0.02 per call · schema v1.0.0

Says whether this exact task, endpoint and parameter set was already paid for recently. For an agent about to retry, that does not want to pay twice for one job.

You provide: policyId (string); taskHash (string); endpoint (string); paramsHash (string).

You receive: whether a matching prior intent exists, which one, and how long the match remains in the window.

**Must already exist**

- A prior call recorded in THIS instance's rolling window. The window is process-local and one hour long, so a match can only be found for something this instance itself saw. Obtain it with: POST /preflight_payment on this same instance, within the last hour.

**What it changes**

- Settles $0.02 of USDT0.

**Repeating an identical request:** read-only.

**Refusals**

- `PAYMENT_REQUIRED` (402) — no valid payment accompanied the request
- `FIELDS_REQUIRED` (400) — any of policyId, taskHash, endpoint or paramsHash is missing

**A request that works**

```json
{
  "policyId": "7",
  "taskHash": "0x1111111111111111111111111111111111111111111111111111111111111111",
  "endpoint": "https://asp.untch.xyz/ping_untch",
  "paramsHash": "0x5555555555555555555555555555555555555555555555555555555555555555"
}
```

**A request that is refused** — `FIELDS_REQUIRED`

```json
{
  "policyId": "7",
  "taskHash": "0x1111111111111111111111111111111111111111111111111111111111111111",
  "endpoint": "https://asp.untch.xyz/ping_untch"
}
```

## Metadata redaction — `redact_payment_metadata`

`POST /redact_payment_metadata` · $0.02 per call · schema v1.0.0

Strips emails, phone numbers, API keys and bearer tokens out of payment metadata and hashes what is left. For an agent that must attach metadata to a payment without publishing what is inside it.

You provide: metadata (object).

You receive: the redacted metadata and a hash of it, with nothing stored.

**What it changes**

- Settles $0.02 of USDT0.
- The raw metadata is not stored anywhere. (does not outlive the request)

**Repeating an identical request:** idempotent.

**Refusals**

- `PAYMENT_REQUIRED` (402) — no valid payment accompanied the request
- `METADATA_REQUIRED` (400) — metadata is absent or is not an object

**A request that works**

```json
{
  "metadata": {
    "note": "ship to ada@example.com",
    "orderId": "A-1"
  }
}
```

**A request that is refused** — `METADATA_REQUIRED`

```json
{
  "metadata": "ada@example.com"
}
```

## Receipt lookup — `log_receipt`

`POST /log_receipt` · free · schema v1.0.0

Reports the anchoring state of a receipt this host wrote. For a caller holding a receiptRef who wants to know whether it has reached the chain.

You provide: receiptId (string).

You receive: the receipt's current state, and its anchoring transaction once it has one.

**Must already exist**

- A receiptRef from an earlier paid call. There is nothing to look up until something has written a receipt. Obtain it with: the receiptRef field of a preflight_payment or verify_delivery response.

**Repeating an identical request:** read-only.

**Refusals**

- `BAD_RECEIPT_ID` (400) — receiptId is not a 32-byte hex string
- `RECEIPT_NOT_FOUND` (404) — no receipt with that id was written by this host
- `RECEIPTS_NOT_CONFIGURED` (503) — this instance has no receipt writer

**A request that works**

```json
{
  "receiptId": "0x7777777777777777777777777777777777777777777777777777777777777777"
}
```

**A request that is refused** — `BAD_RECEIPT_ID`

```json
{
  "receiptId": "0x77"
}
```

## Receipt status — `receipt_status`

`GET /receipt_status/{receiptId}` · free · schema v1.0.0

The same receipt state, as a plain GET a browser or a poller can use. For anything polling for an anchor without wanting to POST.

You provide: Nothing. This service takes no parameters.

You receive: the receipt's state and its anchoring transaction once it has one.

**Must already exist**

- A receiptRef from an earlier paid call. There is nothing to poll until something has written a receipt. Obtain it with: the receiptRef field of a preflight_payment or verify_delivery response.

**Repeating an identical request:** read-only.

**Refusals**

- `BAD_RECEIPT_ID` (400) — the id in the path is not a 32-byte hex string
- `RECEIPT_NOT_FOUND` (404) — no receipt with that id was written by this host
- `RECEIPTS_NOT_CONFIGURED` (503) — this instance has no receipt writer

**A request that works**

```json
null
```

**A request that is refused** — `BAD_RECEIPT_ID`

```json
null
```

## Approval status — `escalation_status`

`GET /escalation_status/{pollRef}` · free · schema v1.0.0

Reports whether a payment that was escalated to a human has been approved or denied yet. For an agent waiting on a human decision it triggered.

You provide: Nothing. This service takes no parameters.

You receive: the pending, approved or denied state of one escalation.

**Must already exist**

- A pollRef from an ESCALATE decision. There is nothing to wait on until a decision escalated. Obtain it with: the escalation reference on a preflight_payment response whose decision was ESCALATE.

**Repeating an identical request:** read-only.

**Refusals**

- `ESCALATION_NOT_CONFIGURED` (503) — this instance has no escalation service wired

**A request that works**

```json
null
```

**A request that is refused** — `ESCALATION_NOT_CONFIGURED`

```json
null
```

## Vendor score — `score_vendor`

`POST /score_vendor` · $0.20 per call · schema v1.0.0

Scores how reliably a vendor has delivered, from receipts this host holds. For a buyer choosing between vendors before committing money to one.

You provide: Any one of: vendorId, endpoint, host, payoutAddress.

You receive: a score with its uncertainty, a lower-confidence bound, and the per-feature breakdown showing which parts were observed and which are cold-start priors.

> **Not listable.** This service cannot be completed by a caller who does not already
> hold something no public route produces:
> - Receipt history for that vendor, held by this host.

**Must already exist**

- Receipt history for that vendor, held by this host. The score is computed from receipts. With none, every feature is a cold-start prior and the answer carries almost no information. **No public route produces this.**

**What it changes**

- Settles $0.20 of USDT0.

**Repeating an identical request:** read-only.

**Refusals**

- `PAYMENT_REQUIRED` (402) — no valid payment accompanied the request
- `VENDOR_ID_REQUIRED` (400) — none of vendorId, endpoint or host was given
- `LISTING_ID_UNRESOLVABLE` (400) — only a marketplace listingId was given; this host cannot resolve one
- `PAYOUT_ADDRESS_MALFORMED` (400) — payoutAddress is not a 20-byte address
- `SCORE_STORE_NOT_CONFIGURED` (503) — this instance has no score store

**A request that works**

```json
{
  "host": "asp.untch.xyz"
}
```

**A request that is refused** — `LISTING_ID_UNRESOLVABLE`

```json
{
  "listingId": "okx-12345"
}
```

## Buyer score — `score_buyer`

`POST /score_buyer` · $0.20 per call · schema v1.0.0

Scores how well a buyer has behaved — paying, not disputing spuriously, not retrying blindly. For a vendor deciding whether to take a job from a buyer they do not know.

You provide: Any one of: buyerId, agentId.

You receive: a score with its uncertainty and the per-feature breakdown behind it.

> **Not listable.** This service cannot be completed by a caller who does not already
> hold something no public route produces:
> - Receipt history for that buyer, held by this host.

**Must already exist**

- Receipt history for that buyer, held by this host. Every observed feature comes from receipts. With none, the answer is priors. **No public route produces this.**

**What it changes**

- Settles $0.20 of USDT0.

**Repeating an identical request:** read-only.

**Refusals**

- `PAYMENT_REQUIRED` (402) — no valid payment accompanied the request
- `BUYER_ID_REQUIRED` (400) — neither buyerId nor agentId was given
- `SCORE_STORE_NOT_CONFIGURED` (503) — this instance has no score store

**A request that works**

```json
{
  "agentId": "6047"
}
```

**A request that is refused** — `BUYER_ID_REQUIRED`

```json
{}
```

## Dispute packet — `generate_dispute_packet`

`POST /generate_dispute_packet` · $0.50 per call · schema v1.0.0

Assembles everything this host recorded about one payment into a single hashed evidence bundle. For either side of a dispute who needs one artefact both parties can check.

You provide: intentRef (string).

You receive: the assembled packet, its hash, and — where a writer key is wired — the transaction that anchored the hash.

> **Not listable.** This service cannot be completed by a caller who does not already
> hold something no public route produces:
> - An intentHash this host has history for.

**Must already exist**

- An intentHash this host has history for. The packet is assembled from receipts, ledger entries and escalations. An unknown intent yields an honestly empty packet. **No public route produces this.**

**What it changes**

- Settles $0.50 of USDT0.
- Anchors the packet hash on-chain, only where this instance holds a writer key.

**Repeating an identical request:** idempotent.

**Refusals**

- `PAYMENT_REQUIRED` (402) — no valid payment accompanied the request
- `INTENT_REF_REQUIRED` (400) — intentRef is absent or is not a 32-byte hex string
- `REPORT_STORE_NOT_CONFIGURED` (503) — this instance has no report store

**A request that works**

```json
{
  "intentRef": "0x1111111111111111111111111111111111111111111111111111111111111111"
}
```

**A request that is refused** — `INTENT_REF_REQUIRED`

```json
{}
```

## Spend reconciliation — `reconcile_agent_spend`

`POST /reconcile_agent_spend` · $0.25 per call · schema v1.0.0

Totals what one agent spent over a period, and what was blocked before it spent. For an operator reconciling an agent's spend against what they authorised.

You provide: agentId (string); period (string or object).

You receive: the period report, its hash, and — where a writer key is wired — the transaction that anchored the hash.

> **Not listable.** This service cannot be completed by a caller who does not already
> hold something no public route produces:
> - Receipt and ledger history for that agent in that period.

**Must already exist**

- Receipt and ledger history for that agent in that period. The report is an aggregation. With no history it is honestly empty rather than wrong. **No public route produces this.**

**What it changes**

- Settles $0.25 of USDT0. Both the day and the week report currently charge this same rate.
- Anchors the report hash on-chain, only where this instance holds a writer key.

**Repeating an identical request:** idempotent.

**Refusals**

- `PAYMENT_REQUIRED` (402) — no valid payment accompanied the request
- `AGENT_ID_REQUIRED` (400) — agentId is absent or is neither a decimal nor a 32-byte hex id
- `PERIOD_INVALID` (400) — the period could not be parsed
- `REPORT_STORE_NOT_CONFIGURED` (503) — this instance has no report store

**A request that works**

```json
{
  "agentId": "6047",
  "period": "day"
}
```

**A request that is refused** — `AGENT_ID_REQUIRED`

```json
{
  "period": "day"
}
```

## Café menu — `cafe_menu`

`GET /cafe/menu` · free · schema v1.0.0

A machine-readable café menu, used to demonstrate governed spending on something ordinary. For an agent being shown what a governed purchase looks like end to end.

You provide: Nothing. This service takes no parameters.

You receive: the items, their prices, and how long a quote holds.

**Repeating an identical request:** read-only.

**Refusals**

- `METHOD_NOT_ALLOWED` (405) — the path is right and the verb is not

**A request that works**

```json
null
```

**A request that is refused** — `METHOD_NOT_ALLOWED`

```json
{}
```

## Café order — `cafe_order_latte`

`POST /cafe/order/latte` · $0.04 per call · schema v1.0.0

Buys a demonstration coffee voucher, to show a real payment against a real policy. For anyone evaluating what a governed agent purchase feels like without spending much.

You provide: Any one of: buyerRef.

You receive: an order id and a pickup code. The fulfilment is a demonstration, not a real coffee.

**What it changes**

- Settles $0.04 of USDT0.
- Issues a demonstration voucher. No coffee is dispatched. (does not outlive the request)

**Repeating an identical request:** not-idempotent.

**Refusals**

- `PAYMENT_REQUIRED` (402) — no valid payment accompanied the request

**A request that works**

```json
{
  "buyerRef": "demo-run-1"
}
```

**A request that is refused** — `PAYMENT_REQUIRED`

```json
{}
```

## Product name suggestions — `suggest_names`

`POST /builder/suggest_names` · $0.01 per call · schema v1.0.0

Proposes short product names for an idea, filtered against a list of over-used startup stems. For a founder or an agent naming something new.

You provide: idea (string).

You receive: a handful of candidate names, each with the reasoning behind it.

**What it changes**

- Settles $0.01 of USDT0.

**Repeating an identical request:** not-idempotent.

**Refusals**

- `PAYMENT_REQUIRED` (402) — no valid payment accompanied the request
- `IDEA_REQUIRED` (400) — idea is absent, empty, or longer than 280 characters

**A request that works**

```json
{
  "idea": "spend control for autonomous agents",
  "count": 6
}
```

**A request that is refused** — `IDEA_REQUIRED`

```json
{
  "count": 6
}
```

## Name ranking — `rank_options`

`POST /builder/rank_options` · free · schema v1.0.0

Orders a list of candidate names by length, character set and how easily they are said. For someone choosing between names they already have.

You provide: names (array).

You receive: the names in ranked order with their scores.

**Repeating an identical request:** idempotent.

**Refusals**

- `NAMES_REQUIRED` (400) — names is absent or empty

**A request that works**

```json
{
  "names": [
    "Untch",
    "Kyrve",
    "FlowLabs",
    "NexaVerse"
  ]
}
```

**A request that is refused** — `NAMES_REQUIRED`

```json
{
  "names": []
}
```

## Domain availability — `check_domains`

`POST /builder/check_domains` · free · schema v1.0.0

Asks the registries whether the domains for a list of names are taken. For someone checking whether a name is usable before committing to it.

You provide: names (array).

You receive: one availability verdict per domain, with the registry response behind it.

**What it changes**

- Queries public domain registries. Nothing is registered or reserved. (does not outlive the request)

**Repeating an identical request:** read-only.

**Refusals**

- `NAMES_REQUIRED` (400) — names is absent or empty

**A request that works**

```json
{
  "names": [
    "untch",
    "kyrve"
  ],
  "tlds": [
    ".com",
    ".xyz"
  ]
}
```

**A request that is refused** — `NAMES_REQUIRED`

```json
{}
```

## Launch checklist — `seo_tips`

`POST /builder/seo_tips` · free · schema v1.0.0

Returns a short launch checklist for a chosen brand name. For someone who has picked a name and is about to launch.

You provide: name (string).

You receive: a list of concrete steps, using the name given.

**Repeating an identical request:** idempotent.

**Refusals**

- `NAME_REQUIRED` (400) — name is absent or empty

**A request that works**

```json
{
  "name": "Untch",
  "idea": "spend control for agents"
}
```

**A request that is refused** — `NAME_REQUIRED`

```json
{
  "idea": "spend control"
}
```

## Launch brand pack — `brand_pack`

`POST /builder/brand_pack` · $0.05 per call · schema v1.0.0

Runs naming, domain checking, ranking and the launch checklist as one call. For a founder or an agent wanting a first pass at a brand in a single request.

You provide: idea (string).

You receive: candidate names, their domain verdicts, a ranking, and a launch checklist.

**What it changes**

- Settles $0.05 of USDT0.
- Queries public domain registries. Nothing is registered or reserved. (does not outlive the request)

**Repeating an identical request:** not-idempotent.

**Refusals**

- `PAYMENT_REQUIRED` (402) — no valid payment accompanied the request
- `IDEA_REQUIRED` (400) — idea is absent, empty, or longer than 280 characters

**A request that works**

```json
{
  "idea": "spend control for autonomous agents"
}
```

**A request that is refused** — `IDEA_REQUIRED`

```json
{}
```

