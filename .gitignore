# secrets — never commit
.env
.env.*
!.env.example

# node
node_modules/
dist/
*.log

# macOS
.DS_Store

# local-only Remotion scratch app for launch footage (also excluded from the pnpm workspace so the
# committed lockfile stays reproducible from a clean checkout)
apps/video/

# next.js build output (apps/web)
.next/
next-env.d.ts
*.tsbuildinfo

# internal PRD stays local per D0.8 (repo scaffold gitignores /internal)
# NOTE: for D0.1 we DO want the notes + evidence tracked locally; see below
/internal/
internal/*