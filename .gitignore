# secrets — never commit
.env
.env.*
!.env.example

# node
node_modules/
dist/
*.log

# macOS
.DS_Store

# local-only Remotion: launch video app + installed skills/scratch (not product build)
apps/video/
.agents/
**/remotion-best-practices/
**/remotion-*/

# next.js build output (apps/web)
.next/
next-env.d.ts
*.tsbuildinfo

# internal PRD stays local per D0.8 (repo scaffold gitignores /internal)
# NOTE: for D0.1 we DO want the notes + evidence tracked locally; see below
/internal/
internal/*